# Peer dependencies — relaxed to allow Storybook and testing tools
# that declare wider peer dep ranges
strict-peer-dependencies=false

# Hoist packages needed by tooling and Storybook
shamefully-hoist=true
